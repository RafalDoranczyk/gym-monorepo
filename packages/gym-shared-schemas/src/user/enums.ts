/* eslint-disable no-unused-vars */

export enum PALETTE_MODES {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum CURRENCIES {
  EUR = 'eur',
  PLN = 'pln',
  USD = 'usd',
}
export enum USER_ROLES {
  ADMIN = 'admin',
  USER = 'user',
}
